<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 min-h-screen">
    <div class="container mx-auto px-4 py-8 max-w-2xl">
      <a
        href="/"
        class="inline-block mb-6 bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded-lg transition-colors"
      >
        Back to Home
      </a>
      <h1 class="text-3xl font-bold text-center mb-8"><%= title %></h1>
      <div class="bg-white rounded-lg shadow-md p-6 mb-6">
        <form id="add-todo-form" class="flex gap-2">
          <input
            type="text"
            id="todo-input"
            placeholder="Enter new task..."
            class="flex-1 px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
            required
            autofocus
          />
          <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-6 rounded-lg">
            Add
          </button>
        </form>
      </div>

      <div id="todos-container" class="space-y-3"></div>
    </div>

    <script src="/js/app.js"></script>
  </body>
</html>
